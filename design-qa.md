# Design QA — Note 2.4.0

这是版本验收记录，不是现役产品规则。产品边界见 `docs/PRODUCT.md`，当前功能与命令见 `README.md`。

## 视觉基线

2.3.0 的组织与全屏改版截图保存在 `qa/2.3.0/`：

- `reference-notes.png`：用户参考图
- `implementation-1430x1782.png`：同尺寸双栏
- `implementation-1440.png`：最大化宽屏
- `implementation-544.png`：窄窗单栏

截图使用无私人数据的 reference fixture。正文内容可以不同，验收关注导航层级、工具栏、列表密度、编辑器结构和窗口适配。

## 2.4.0 验收结论

- 420×900 最小宽度附近仍保持单栏编辑器，常用工具栏完整显示，低频格式留在弹层。
- 笔记可从列表更多按钮或右键菜单移入未分类、笔记本根目录及不同一级文件夹，移动后继续打开原笔记。
- `A↓` / `A↑` 与 `Ctrl + [` / `Ctrl + ]` 按固定字号阶梯工作，实测可在 14px、16px 间往返。
- 行距 2.0 生成真实段落 `line-height: 2`；结构化 JSON 保存该属性，Markdown 不携带视觉元数据。
- 真实 Electron 最大化/还原、富文本字体、公式、行距、旧笔记迁移与安装版加载均通过。
- 2026-07-21 验收时：81/81 自动化测试通过，生产构建通过，浏览器主流程和安装版 smoke 无 renderer exception。

## 已知非阻断项

Notes 的 Tiptap 与 KaTeX 仍是按需加载的独立代码块，约 247.5KB gzip；它不进入 Todo 首屏。只有继续显著扩充编辑器能力时，才需要拆分公式或导入模块。

最终结论：passed。
